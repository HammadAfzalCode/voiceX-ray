// Implemented in Phase 5 — tool call cards.
export class ToolCards {
  addCard(_callId: string, _name: string, _args: unknown): void {
    // Phase 5
  }

  fillResult(_callId: string, _result: unknown): void {
    // Phase 5
  }

  markError(_callId: string): void {
    // Phase 5
  }
}
